# Base URL of the Laravel API (must include the trailing /api).
VITE_API_URL=http://127.0.0.1:8000/api

# Google OAuth 2.0 Web Client ID (from Google Cloud Console > APIs & Services > Credentials).
# Used by the "Continue with Google" button. Leave empty to disable Google sign-in.
VITE_GOOGLE_CLIENT_ID=
